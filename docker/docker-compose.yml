version: "3.7"
services:
  mock-service:
    image: mock-service
    build:
      dockerfile: Dockerfile
      context: ./mock_service
    command: /entrypoint.sh
    env_file:
        - .env
    ports:
     - "8080:8080"
     - "8786:8786"

